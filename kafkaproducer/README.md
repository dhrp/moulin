# How to start the development Kafka server:

clone the repo:
https://hub.docker.com/r/wurstmeister/kafka/

then run `docker-compose up -d`

Make sure that when you change the IP address, you remove and restart
